% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_overlap_ml.R
\name{plot_overlap_ml}
\alias{plot_overlap_ml}
\title{Plot propensity score overlap between treatment groups}
\usage{
plot_overlap_ml(
  x,
  treatment = NULL,
  cluster = NULL,
  ps = NULL,
  facet_clusters = FALSE,
  top_n_clusters = 12L
)
}
\arguments{
\item{x}{An \code{ml_ps} object from \code{\link{ml_ps}}, or a plain
\code{data.frame} (in which case \code{treatment}, \code{cluster}, and
\code{ps} must be supplied).}

\item{treatment}{Character string. Treatment variable name. Required when
\code{x} is a \code{data.frame}.}

\item{cluster}{Character string. Cluster variable name. Required when
\code{x} is a \code{data.frame}.}

\item{ps}{Character string. Propensity score variable name. Required when
\code{x} is a \code{data.frame}.}

\item{facet_clusters}{Logical. Facet by cluster? Default \code{FALSE}.}

\item{top_n_clusters}{Positive integer. Maximum clusters shown when
\code{facet_clusters = TRUE}. Default \code{12}.}
}
\value{
A \code{\link[ggplot2]{ggplot}} object.
}
\description{
Creates a density overlap plot of estimated propensity scores by treatment
group. Substantial overlap supports the positivity assumption. The plot can
be shown overall or faceted by cluster.
}
\examples{
dat <- simulate_ml_data(n_clusters = 5, cluster_size = 10, seed = 1)
ps  <- ml_ps(dat, "z", c("x1", "x2", "x3"), "school_id")
plot_overlap_ml(ps)

}
\seealso{
\code{\link{ml_ps}}
}
